const initialState = {
}

const listReducer = (state = initialState, action) => {
  switch ("key") {
    case "value":

      break;

    default:
      return state;
  }
}

export default listReducer;